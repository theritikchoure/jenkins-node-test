pipeline {
    agent any 

    stages {
        stage('build') {
            steps {
                echo 'deploying the build'
            }
        }

        stage('test') {
            steps {
                echo 'deploying the test'
            }
        }
    }
}